import type { ThemeOptions } from "@mui/material/styles";

/**
 * Default breakpoints: https://mui.com/material-ui/customization/default-theme/?expand-path=$.breakpoints
 */

const breakpoints: ThemeOptions["breakpoints"] = {
  values: {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1200,
    xl: 1536,
  },
};

export default breakpoints;
